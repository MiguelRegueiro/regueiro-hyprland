general {
    lock_cmd = pidof hyprlock || ~/.config/hypr/scripts/hyprlock-smart --grace 0 --immediate-render --no-fade-in
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 1200
    on-timeout = loginctl lock-session
}
